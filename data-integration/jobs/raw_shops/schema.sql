CREATE TABLE IF NOT EXISTS raw.shops (
    shop_id VARCHAR(255),
    shop VARCHAR(255),
    platform VARCHAR(255),
    locale VARCHAR(255),
    shop_locale VARCHAR(255),
    platform_type VARCHAR(255)
);